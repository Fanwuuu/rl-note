TD-Learning是model-free的方法。
蒙特卡洛方法是一种non-incremental的方法，而TD-Learning是一种**增量的（Incremental）** 方法
***
我们先从RM算法开始，逐渐过渡到TD-Learning。
我们接下来考虑一些随机问题，并展示如何使用 RM 算法来求解它们。

首先，考虑简单的**均值估计问题（Mean Estimation Problem）**：基于 \(X\) 的一些独立同分布（iid）样本 \(\{x\}\)，计算

$$
w = \mathbb{E}[X],
$$

* 通过令 \(g(w) = w - \mathbb{E}[X]\)，我们可以将问题重新表述为一个求根问题

    $$
    g(w) = 0.
    $$

* 由于我们只能获得 \(X\) 的样本 \(\{x\}\)，带噪观测为

    $$
    \tilde{g}(w, \eta) = w - x = (w - \mathbb{E}[X]) + (\mathbb{E}[X] - x) \triangleq g(w) + \eta.
    $$

* 那么，根据上一讲的内容，我们知道用于求解 \(g(w) = 0\) 的 RM 算法为

    $$
    w_{k+1} = w_k - \alpha_k \tilde{g}(w_k, \eta_k) = w_k - \alpha_k(w_k - x_k)
    $$
>这里的公式，前面也提到过。

考虑一个更复杂的问题：计算

$$
w = \mathbb{E}[R + \gamma v(X)],
$$

其中 \(R, X\) 是随机变量，\(\gamma\) 是常数，且 \(v(\cdot)\) 是一个函数。
* 假设我们可以获得 \(X\) 和 \(R\) 的样本 \(\{x\}\) 和 \(\{r\}\)。我们定义

    $$
    \begin{aligned}
    g(w) &= w - \mathbb{E}[R + \gamma v(X)], \\
    \tilde{g}(w, \eta) &= w - [r + \gamma v(x)] \\
    &= (w - \mathbb{E}[R + \gamma v(X)]) + (\mathbb{E}[R + \gamma v(X)] - [r + \gamma v(x)]) \\
    &\triangleq g(w) + \eta.
    \end{aligned}
    $$

* 那么，问题变为一个求根问题：\(g(w) = 0\)。相应的 RM 算法为

    $$
    w_{k+1} = w_k - \alpha_k \tilde{g}(w_k, \eta_k) = w_k - \alpha_k[w_k - (r_k + \gamma v(x_k))]
    $$
>这个$R+\gamma v(X)$看着就有点像状态价值了。区别在于，RM的目标函数$v(X)$是不变的，而TD-Learning的目标函数$v(X)$一直在变。TD 可以看作 RM 在“自举目标”下的推广。
***
**算法所需的数据/经验**：
* \((s_0, r_1, s_1, \dots, s_t, r_{t+1}, s_{t+1}, \dots)\) 或 \(\{(s_t, r_{t+1}, s_{t+1})\}_t\)，遵循给定的策略 \(\pi\) 生成。

**TD 学习算法（TD Learning Algorithm）** 为

$$
\begin{cases}
v_{t+1}(s_t) = v_t(s_t) - \alpha_t(s_t) \Big[ v_t(s_t) - [r_{t+1} + \gamma v_t(s_{t+1})] \Big], & (1) \\
v_{t+1}(s) = v_t(s),  \forall s \neq s_t, & (2)
\end{cases}
$$

其中 \(t = 0, 1, 2, \dots\)。这里，\(v_t(s_t)\) 是 \(v_\pi(s_t)\) 的估计状态价值；\(\alpha_t(s_t)\) 是 \(s_t\) 在时刻 \(t\) 的学习率。
* 在时刻 \(t\)，只有被访问状态 \(s_t\) 的价值被更新，而未访问状态 \(s \neq s_t\) 的价值保持不变。
* 当上下文清晰时，式 (2) 中的更新将被省略。

TD 算法可以标注为

$$
\underbrace{v_{t+1}(s_t)}_{\text{新估计}} = \underbrace{v_t(s_t)}_{\text{当前估计}} - \alpha_t(s_t) \Bigg[ \underbrace{v_t(s_t) - \overbrace{[r_{t+1} + \gamma v_t(s_{t+1})]}^{\text{TD target } \bar{v}_t}}_{\text{TD error } \delta_t} \Bigg], \quad (3)
$$

这里，

$$
\bar{v}_t \triangleq r_{t+1} + \gamma v(s_{t+1})
$$

被称为 **TD 目标（TD Target）**。

$$
\delta_t \triangleq v(s_t) - [r_{t+1} + \gamma v(s_{t+1})] = v(s_t) - \bar{v}_t
$$

被称为 **TD 误差（TD Error）**。
显然，新估计 \(v_{t+1}(s_t)\) 是当前估计 \(v_t(s_t)\) 和 TD 误差的组合。
>TD-Learning是先有公式，再有证明。大部分情况下，数学和算法发展中总是先有公式，后有证明。
>TD 更新本质上是“带噪声的贝尔曼算子不动点迭代”；因为贝尔曼算子是压缩的，所以用当前估计去算目标、再更新当前估计，误差会被逐步压缩掉，最终收敛到真实价值。
***
**首先，为什么 \(\bar{v}_t\) 被称为 TD 目标（TD Target）？**
那是因为该算法驱动 \(v(s_t)\) 趋向于 \(\bar{v}_t\)。
为了看清这一点，

$$
\begin{aligned}
&& v_{t+1}(s_t) &= v_t(s_t) - \alpha_t(s_t)[v_t(s_t) - \bar{v}_t] \\
\implies && v_{t+1}(s_t) - \bar{v}_t &= v_t(s_t) - \bar{v}_t - \alpha_t(s_t)[v_t(s_t) - \bar{v}_t] \\
\implies && v_{t+1}(s_t) - \bar{v}_t &= [1 - \alpha_t(s_t)][v_t(s_t) - \bar{v}_t] \\
\implies && |v_{t+1}(s_t) - \bar{v}_t| &= |1 - \alpha_t(s_t)||v_t(s_t) - \bar{v}_t|
\end{aligned}
$$

由于 \(\alpha_t(s_t)\) 是一个小的正数，我们有

$$
0 < 1 - \alpha_t(s_t) < 1
$$

因此，

$$
|v_{t+1}(s_t) - \bar{v}_t| \le |v_t(s_t) - \bar{v}_t|
$$

这意味着 \(v(s_t)\) 被驱动趋向于 \(\bar{v}_t\)！
>$a_t(s_t)=1$时，$v_{t+1}(s_t)$就是TD Target；$a_t(s_t)=0$时，$v_{t+1}(s_t)$不变。$a_t(s_t)$代表了更新的幅度。
***
**其次，TD 误差的解释是什么？**

$$
\delta_t = v(s_t) - [r_{t+1} + \gamma v(s_{t+1})]
$$

* 它是两个连续时间步之间的差。
* 它反映了 \(v_t\) 和 \(v_\pi\) 之间的不足。为了看清这一点，记

    $$
    \delta_{\pi,t} \triangleq v_\pi(s_t) - [r_{t+1} + \gamma v_\pi(s_{t+1})]
    $$

    注意

    $$
    \mathbb{E}[\delta_{\pi,t}|S_t = s_t] = v_\pi(s_t) - \mathbb{E}[R_{t+1} + \gamma v_\pi(S_{t+1})|S_t = s_t] = 0.
    $$

* 如果 \(v_t = v_\pi\)，那么 \(\delta_t\) 应该为零（在期望意义上）。
* 因此，如果 \(\delta_t\) 不为零，那么 \(v_t\) 不等于 \(v_\pi\)。
* TD 误差可以解释为**新的信息（Innovation）**，这意味着从**经验（Experience）** \((s_t, r_{t+1}, s_{t+1})\) 中获得的新信息。
>注意，即使$v_t\neq v_\pi$，TD-Error也有可能等于0。TD 误差为 0 只说明“这一步没有新信息”，不说明“已经收敛到最优”。
***
其他性质：
* (3) 式中的 TD 算法**仅估计给定策略（Given Policy）的状态价值**。
    *   它不估计动作价值（Action Values）。
    *   它不搜索最优策略（Optimal Policies）。
>在model-free的情况下，想通过状态价值得到动作价值是不可能的，就难以更新策略。在第五课中使用了通过多次采样直接估计动作价值的方法来更新策略。
***
TD算法是在求解一个给定策略的贝尔曼公式。
TD算法是在没有模型的情况下求解贝尔曼公式。