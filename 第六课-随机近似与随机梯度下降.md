TD Learning是**随机近似（Stochastic Approximation）** 的一种特殊情况。
***
如何计算均值 \(\bar{x}\)？

$$
\mathbb{E}[X] \approx \bar{x} := \frac{1}{N} \sum_{i=1}^N x_i.
$$

我们有两种方法。
* **第一种方法**，收集所有样本然后计算平均值。
    * 这种方法的**缺点**是，如果样本是在一段时间内逐个收集的，我们必须等到所有样本都被收集完毕。
* **第二种方法**可以避免这个缺点，因为它以一种**增量式（Incremental）** 和**迭代式（Iterative）** 的方式计算平均值。

特别地，假设

$$
w_{k+1} = \frac{1}{k} \sum_{i=1}^k x_i, \quad k = 1, 2, \dots
$$

因此

$$
w_k = \frac{1}{k-1} \sum_{i=1}^{k-1} x_i, \quad k = 2, 3, \dots
$$

那么，\(w_{k+1}\) 可以用 \(w_k\) 表示为

$$
\begin{aligned}
w_{k+1} &= \frac{1}{k} \sum_{i=1}^k x_i = \frac{1}{k} \left( \sum_{i=1}^{k-1} x_i + x_k \right) \\
&= \frac{1}{k} ((k-1)w_k + x_k) = w_k - \frac{1}{k}(w_k - x_k).
\end{aligned}
$$
因此，我们得到以下迭代式：
$$
w_{k+1}=w_k - \frac{1}{k}(w_k - x_k)
$$
此外，考虑一个具有更一般表达式的算法：

$$
w_{k+1} = w_k - \alpha_k(w_k - x_k),
$$

其中 \(1/k\) 被替换为 \(\alpha_k > 0\)。
* 这个算法是否仍然收敛到均值 \(\mathbb{E}[X]\)？我们将证明，如果 \(\{\alpha_k\}\) 满足一些温和的条件，答案是肯定的。
* 我们还将证明，这个算法是一个特殊的 **SA 算法（SA Algorithm）**，也是一个特殊的**随机梯度下降算法（Stochastic Gradient Descent Algorithm）**。
* 在下一讲中，我们将看到**时序差分算法（Temporal-Difference Algorithms）** 具有相似（但更复杂）的表达式。
***
**随机逼近（Stochastic Approximation, SA）**：
* SA 指的是一大类解决求根或优化问题的随机迭代算法。
* 与许多其他求根算法（例如基于梯度的方法）相比，SA 的强大之处在于它**不需要知道目标函数的表达式，也不需要知道其导数**。

**Robbins-Monro (RM) 算法**：
* 它是随机逼近领域的开创性工作。
* 著名的**随机梯度下降算法（Stochastic Gradient Descent Algorithm）** 是 RM 算法的一种特殊形式。
* 它可用于分析开头介绍的那个增量式的均值估计算法。
***
**问题陈述（Problem Statement）**：假设我们想要找到方程

$$
g(w) = 0,
$$

的根，其中 \(w \in \mathbb{R}\) 是待求解的变量，且 \(g: \mathbb{R} \to \mathbb{R}\) 是一个函数。
在已知$g$的表达式或导数的情况下，有很多数值的算法去求解这个问题，但如果表达式未知，例如$g$是一个神经网络（当然，严格来说神经网络的表达式是已知的，因为能计算反向传播的梯度），就需要用RM算法求解。
Robbins-Monro (RM) 算法可以解决这个问题：

$$
w_{k+1} = w_k - a_k \tilde{g}(w_k, \eta_k), \quad k = 1, 2, 3, \dots
$$

其中
* \(w_k\) 是根的第 \(k\) 次估计值
* \(\tilde{g}(w_k, \eta_k) = g(w_k) + \eta_k\) 是第 \(k\) 次带噪声的观测值
* \(a_k\) 是一个正系数。

函数 \(g(w)\) 是一个**黑盒**！该算法依赖于数据：
* 输入序列：\(\{w_k\}\)
* 带噪声的输出序列：\(\{\tilde{g}(w_k, \eta_k)\}\)

**原则**：如果没有模型，我们就需要数据。
* 在这里，模型指的是函数的表达式。
***
一个说明性示例：
* \(g(w) = \tanh(w - 1)\)
* \(g(w) = 0\) 的真实根是 \(w^* = 1\)。
* 参数：\(w_1 = 3\)，\(a_k = 1/k\)，\(\eta_k \equiv 0\)（为了简单起见，无噪声）

在这种情况下，RM 算法为

$$
w_{k+1} = w_k - a_k g(w_k)
$$

其中，$a_k$序列的取值有一定要求，这里的$a_k=1/k$其实是一个经典的步长选择。这里为了简洁起见不进行说明，严格的数学收敛性证明详见视频。
直观理解：\(w_{k+1}\) 比 \(w_k\) 更接近 \(w^*\)。
* 当 \(w_k > w^*\) 时，我们有 \(g(w_k) > 0\)。那么，\(w_{k+1} = w_k - a_k g(w_k) < w_k\)，因此 \(w_{k+1}\) 比 \(w_k\) 更接近 \(w^*\)。
* 当 \(w_k < w^*\) 时，我们有 \(g(w_k) < 0\)。那么，\(w_{k+1} = w_k - a_k g(w_k) > w_k\)，并且 \(w_{k+1}\) 比 \(w_k\) 更接近 \(w^*\)。
>Tips:不动点迭代法不是 Robbins-Monro 算法，但它是 Robbins-Monro 算法在无噪声、步长恒为 1 时的特例。Robbins-Monro 是不动点迭代法的随机化、带步长的推广。读者可以试着取
$$
x=g(x)\\
x-g(x)=0
$$
按照RM算法的格式推导一下。
***
接下来，我们介绍**随机梯度下降（Stochastic Gradient Descent, SGD）算法**：
* SGD 广泛应用于机器学习领域以及 RL 中。
* SGD 是一种特殊的 RM 算法。
* 均值估计算法是一种特殊的 SGD 算法。

假设我们旨在求解以下优化问题：

$$
\min_w J(w) = \mathbb{E}[f(w, X)]
$$

* \(w\) 是待优化的参数。
* \(X\) 是一个随机变量。期望是关于 \(X\) 求的。
* \(w\) 和 \(X\) 可以是标量或向量。函数 \(f(\cdot)\) 是一个标量。.

> 可以将梯度下降对应到RM算法的框架中，我们求解的是$\nabla J(w)=0$，设$\nabla J(w)=g(w)$，记 \(\tilde{g}(w_k)\) 为 \(g(w_k)\) 的带噪观测，即$\tilde{g}(w_k) = g(w_k) + \varepsilon_k$，则RM的更新公式就是$w_{k+1}=w_k-a_k\tilde{g}(w_k)$。
>除此之外，后文也会给出具体的证明。

 **方法 1：梯度下降（Gradient Descent, GD）**

$$
w_{k+1} = w_k - \alpha_k \nabla_w \mathbb{E}[f(w_k, X)] = w_k - \alpha_k \mathbb{E}[\nabla_w f(w_k, X)]
$$

缺点：期望值难以获得。

**方法 2：批量梯度下降（Batch Gradient Descent, BGD）**

$$
\mathbb{E}[\nabla_w f(w_k, X)] \approx \frac{1}{n} \sum_{i=1}^n \nabla_w f(w_k, x_i).
$$

$$
w_{k+1} = w_k - \alpha_k \frac{1}{n} \sum_{i=1}^n \nabla_w f(w_k, x_i).
$$

缺点：对于每个 \(w_k\)，每次迭代都需要大量样本。

**方法 3：随机梯度下降（Stochastic Gradient Descent, SGD）**

$$
w_{k+1} = w_k - \alpha_k \nabla_w f(w_k, x_k),
$$

* 与梯度下降方法相比：将**真实梯度（True Gradient）** \(\mathbb{E}[\nabla_w f(w_k, X)]\) 替换为**随机梯度（Stochastic Gradient）** \(\nabla_w f(w_k, x_k)\)。
* 与批量梯度下降方法相比：令 \(n = 1\)。
***
#### 例子
对于以下这个示例
$$
\min_w J(w) = \mathbb{E}[f(w, X)] = \mathbb{E}\left[ \frac{1}{2}\|w - X\|^2 \right],
$$

其中

$$
f(w, X) = \|w - X\|^2/2 \qquad \nabla_w f(w, X) = w - X
$$
* 用于求解上述问题的 GD 算法为

    $$
    \begin{aligned}
    w_{k+1} &= w_k - \alpha_k \nabla_w J(w_k) \\
    &= w_k - \alpha_k \mathbb{E}[\nabla_w f(w_k, X)] \\
    &= w_k - \alpha_k \mathbb{E}[w_k - X].
    \end{aligned}
    $$

* 用于求解上述问题的 SGD 算法为

    $$
    w_{k+1} = w_k - \alpha_k \nabla_w f(w_k, x_k) = w_k - \alpha_k(w_k - x_k)
    $$

* 注意：
    * 这与我们之前介绍的均值估计算法相同，也就是说均值估计算法是一种特殊的 SGD 算法。
>说起来，回归问题和优化问题之间可以互相转化。例如最小二乘法就是通过定义一个损失函数把回归问题转化为优化问题（最小化损失函数）来求解。
>不过这里并不是回归问题，而是求根问题。求根找的是一个点$x$使得$f(x)=0$，而回归找的是一个函数$g(x)\approx y$。
***
#### 随机梯度下降的收敛性证明
我们接下来将证明 SGD 是一种特殊的 RM 算法。那么，收敛性自然就随之而来。

SGD 的目标是最小化

$$
J(w) = \mathbb{E}[f(w, X)]
$$

这个问题可以转化为一个求根问题：

$$
\nabla_w J(w) = \mathbb{E}[\nabla_w f(w, X)] = 0
$$

令

$$
g(w) = \nabla_w J(w) = \mathbb{E}[\nabla_w f(w, X)].
$$

那么，SGD 的目标就是找到 \(g(w) = 0\) 的根。
我们可以测量的是

$$
\begin{aligned}
\tilde{g}(w, \eta) &= \nabla_w f(w, x) \\
&= \underbrace{\mathbb{E}[\nabla_w f(w, X)]}_{g(w)} + \underbrace{\nabla_w f(w, x) - \mathbb{E}[\nabla_w f(w, X)]}_{\eta}.
\end{aligned}
$$

即$\nabla_w f(w, x)$这个采样本身就是真值加上一个噪声$g(w)+\eta$，其本身就是$g(w)$一个有噪声的观测$\tilde{g}(w,\eta)$。那么，用于求解 \(g(w) = 0\) 的 RM 算法为

$$
w_{k+1} = w_k - a_k \tilde{g}(w_k, \eta_k) = w_k - a_k \nabla_w f(w_k, x_k)
$$

* 它正是 SGD 算法。因此，SGD 是一种特殊的 RM 算法。

接下来只要证明它满足RM算法收敛的条件即可，不再赘述。
***
#### 随机梯度下降有趣的性质
如果$f$是**强凸函数**，在参数 \(w_k\) 离最优参数 \(w^\ast\) 较远时，\(w_k\) 的梯度大致指向 \(w^\ast\)，相对误差较小。
设SGD的梯度和BGD的相对误差为：
$$
\delta_k \doteq \frac{|\nabla_w f(w_k, x_k) - \mathbb{E}[\nabla_w f(w_k, X)]|}{|\mathbb{E}[\nabla_w f(w_k, X)]|}
$$
由于 \(\mathbb{E}[\nabla_w f(w^*, X)] = 0\)，我们进一步有

$$
\delta_k = \frac{|\nabla_w f(w_k, x_k) - \mathbb{E}[\nabla_w f(w_k, X)]|}{|\mathbb{E}[\nabla_w f(w_k, X)] - \mathbb{E}[\nabla_w f(w^*, X)]|} = \frac{|\nabla_w f(w_k, x_k) - \mathbb{E}[\nabla_w f(w_k, X)]|}{|\mathbb{E}[\nabla_w^2 f(\tilde{w}_k, X)(w_k - w^*)]|}
$$

其中最后一个等式源于拉格朗日中值定理，且\(\tilde{w}_k \in [w_k, w^*]\)。
假设 \(f\) 是严格凸的，使得

$$
\nabla_w^2 f \ge c > 0
$$

对于所有 \(w, X\) 成立，其中 \(c\) 是一个正界。
那么，\(\delta_k\) 的分母变为

$$
\begin{aligned}
|\mathbb{E}[\nabla_w^2 f(\tilde{w}_k, X)(w_k - w^*)]| &= |\mathbb{E}[\nabla_w^2 f(\tilde{w}_k, X)](w_k - w^*)| \\
&= |\mathbb{E}[\nabla_w^2 f(\tilde{w}_k, X)]||(w_k - w^*)| \ge c|w_k - w^*|.
\end{aligned}
$$

将上述不等式代入 \(\delta_k\) 得到

$$
\delta_k \le \frac{|\nabla_w f(w_k, x_k) - \mathbb{E}[\nabla_w f(w_k, X)]|}{c|w_k - w^*|}.
$$
注意

$$
\delta_k \le \frac{|\overbrace{\nabla_w f(w_k, x_k)}^{\text{stochastic gradient}} - \overbrace{\mathbb{E}[\nabla_w f(w_k, X)]}^{\text{true gradient}}|}{\underbrace{c|w_k - w^*|}_{\text{distance to the optimal solution}}}.
$$

上述方程揭示了 SGD 一个有趣的收敛模式。
* 相对误差 \(\delta_k\) 与 \(|w_k - w^*|\) 成反比。
* 当 \(|w_k - w^*|\) 很大时，\(\delta_k\) 很小，SGD 的行为类似于 GD。
* 当 \(w_k\) 接近 \(w^*\) 时，相对误差可能很大，并且收敛过程在 \(w^*\) 的邻域内表现出更多的随机性。
***
对于没有随机变量的公式形式
$$
\min_w J(w) = \frac{1}{n} \sum_{i=1}^n f(w, x_i),
$$
特别地，假设 \(X\) 是定义在集合 \(\{x_i\}_{i=1}^n\) 上的一个随机变量。假设其概率分布是均匀的，使得

$$
p(X = x_i) = 1/n
$$

那么，这个确定性优化问题就变成了一个随机优化问题：

$$
\min_w J(w) = \frac{1}{n} \sum_{i=1}^n f(w, x_i) = \mathbb{E}[f(w, X)].
$$