#### 值迭代（Value Iteration）
在第三课提到的不动点迭代法求解贝尔曼最优公式
$$
v_{k+1} = f(v_k) = \max_\pi(r_\pi + \gamma P_\pi v_k), \quad k = 1, 2, 3 \dots
$$

其中 \(v_0\) 可以是任意值

这个算法也叫作**值迭代**。
* **步骤 1：策略更新（Policy Update）**。这一步是为了求解

$$
\pi_{k+1} = \arg\max_\pi(r_\pi + \gamma P_\pi v_k)
$$

其中 \(v_k\) 是给定的。
* **步骤 2：价值更新（Value Update）**。

$$
v_{k+1} = r_{\pi_{k+1}} + \gamma P_{\pi_{k+1}} v_k
$$
具体而言，步骤一用贪心策略，为每个状态选定一个动作价值$q(s,a)$最大的动作作为策略$\pi_{k+1}$，步骤二再利用$\pi_{k+1}$更新每个状态的状态价值，得到$v_{k+1}$
总的来说，上面的两步可以直接合并为
$$
v_{k+1}(s)=\max_a q_k(a,s)
$$
***
#### 策略迭代（Policy Iteration）
给定一个随机的初始策略 \(\pi_0\)，
* **步骤 1：策略评估（Policy Evaluation, PE）**
    这一步是为了计算 \(\pi_k\) 的状态价值

    $$
    v_{\pi_k} = r_{\pi_k} + \gamma P_{\pi_k} v_{\pi_k}
    $$

    注意，\(v_{\pi_k}\) 是一个状态价值函数。
* **步骤 2：策略改进（Policy Improvement, PI）**

    $$
    \pi_{k+1} = \arg\max_\pi(r_\pi + \gamma P_\pi v_{\pi_k})
    $$
对于步骤1，使用第二课提到的迭代法求解贝尔曼公式，得到策略$\pi$下的状态价值，步骤2同值迭代的步骤1
***
#### 截断策略迭代（Truncated Policy Iteration）
从上可知，两者的区别除了初始值，主要区别在于策略评估（即贝尔曼公式的迭代法求解）时的迭代次数，值迭代只迭代一步，策略迭代则迭代无穷多步。
若只迭代$n$步，则该算法叫做**截断策略迭代**。
策略迭代在实际中，若$||v_\pi^{i+1}-v_\pi^i||$小于一个阈值时则停止，等价于阶段策略迭代。

在策略评估步骤中，截断不会破坏算法的收敛性。