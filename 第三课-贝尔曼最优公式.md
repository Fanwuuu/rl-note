如果
$$
v_{\pi_1}(s)\geq v_{\pi_2}(s)\text{ for all } s\in\mathcal S
$$

则称策略$\pi_1$比$\pi_2$更好
**定义**
如果对于所有的 \(s\) 以及任何其他策略 \(\pi\)，都有 \(v_{\pi^*}(s) \ge v_\pi(s)\)，那么策略 \(\pi^*\) 就是**最优策略（Optimal Policy）**
***
在贝尔曼公式前加上$\max_{\pi}$，即得到贝尔曼最优公式，形如
$$
\begin{aligned}
v(s) &= \max_\pi \sum_a \pi(a|s) \left( \sum_r p(r|s,a)r + \gamma \sum_{s'} p(s'|s,a)v(s') \right), \quad \forall s \in \mathcal{S} \\
&= \max_\pi \sum_a \pi(a|s)q(s,a) \quad s \in \mathcal{S}
\end{aligned}
$$
矩阵向量形式则是
$$
v = \max_\pi (r_\pi + \gamma P_\pi v)
$$

其中，对应于 \(s\) 或 \(s'\) 的元素为

$$
[r_\pi]_s \triangleq \sum_a \pi(a|s) \sum_r p(r|s, a)r,
$$
$$
[P_\pi]_{s,s'} = p(s'|s) \triangleq \sum_a \pi(a|s) \sum_{s'} p(s'|s, a)
$$
这里的 \(\max_\pi\) 是逐元素执行的。
***
那么，如何求解贝尔曼最优公式？
考虑到 \(\sum_a \pi(a|s) = 1\)，我们有

$$
\max_\pi \sum_a \pi(a|s)q(s, a) = \max_{a \in \mathcal{A}(s)} q(s, a),
$$

其中，当满足以下条件时达到最优：

$$
\pi(a|s) = \begin{cases} 1 & a = a^* \\ 0 & a \neq a^* \end{cases}
$$

其中 \(a^* = \arg\max_a q(s, a)\)。
对于矩阵向量形式，有\(v = \max_\pi(r_\pi + \gamma P_\pi v)\)。令

$$
f(v) := \max_\pi(r_\pi + \gamma P_\pi v)
$$

那么，贝尔曼最优方程（Bellman Optimality Equation）变为

$$
v = f(v)\tag{1}
$$

对于某个状态$s$，有

$$
[f(v)]_s = \max_\pi \sum_a \pi(a|s)q(s, a), \quad s \in \mathcal{S}
$$
对于(1)式的形式，可以用不动点迭代法求解
贝尔曼最优公式就是贝尔曼公式中的$\pi$为最优策略$\pi^*$时的特殊情况
***
#### 最优策略的一些性质
1.强化学习的奖励具有仿射变换不变性
例如，

$$
r_{\text{boundary}} = r_{\text{forbidden}} = -1, \quad r_{\text{target}} = 1
$$

变为

$$
r_{\text{boundary}} = r_{\text{forbidden}} = 0, \quad r_{\text{target}} = 2, \quad r_{\text{otherstep}} = 1
$$

最优策略保持不变

2.因为折扣因子$\gamma$的存在，即使不存在每走一步给惩罚的情况，模型也不会绕远路