考虑以下多步 trajectory：
$$
S_t \xrightarrow{A_t} R_{t+1}, S_{t+1} \xrightarrow{A_{t+1}} R_{t+2}, S_{t+2} \xrightarrow{A_{t+2}} R_{t+3}, \dots
$$
折扣回报为
$$
G_t = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \dots
$$
\(G_t\) 的期望被定义为**状态价值函数（State-Value Function）** 或简称为状态价值：
$$
v_\pi(s) = \mathbb{E}[G_t|S_t = s]
$$
***
显然，$G_t=R_{t+1}+\gamma G_{t+1}$
那么
$$
\begin{aligned}
v_\pi(s)&=\mathbb E[R_{t+1}|S_t=s]+\gamma\mathbb E[G_{t+1}|S_t=s]\\
&=\sum_a \pi(a|s) \left[ \sum_r p(r|s, a)r + \gamma \sum_{s'} p(s'|s, a)v_\pi(s') \right]
\end{aligned}\tag{1}
$$
此即为**贝尔曼公式（Bellman Equation）**
***
让$r_\pi(s_i)=\mathbb E[R_{t+1}|S_t=s_i]$，可以写成矩阵向量的形式，即
$$v_\pi=r_\pi+\gamma P_\pi v_\pi$$
其中

* \(v_\pi = [v_\pi(s_1), \dots, v_\pi(s_n)]^T \in \mathbb{R}^n\)
* \(r_\pi = [r_\pi(s_1), \dots, r_\pi(s_n)]^T \in \mathbb{R}^n\)
* \(P_\pi \in \mathbb{R}^{n \times n}\)，其中 \([P_\pi]_{ij} = p_\pi(s_j|s_i)\)，是**状态转移矩阵（State Transition Matrix）**

例如，在有4个状态时，可以写成：

$$
\underbrace{\begin{bmatrix} 
v_\pi(s_1) \\ 
v_\pi(s_2) \\ 
v_\pi(s_3) \\ 
v_\pi(s_4) 
\end{bmatrix}}_{v_\pi} 
= 
\underbrace{\begin{bmatrix} 
r_\pi(s_1) \\ 
r_\pi(s_2) \\ 
r_\pi(s_3) \\ 
r_\pi(s_4) 
\end{bmatrix}}_{r_\pi} 
+ \gamma 
\underbrace{\begin{bmatrix} 
p_\pi(s_1|s_1) & p_\pi(s_2|s_1) & p_\pi(s_3|s_1) & p_\pi(s_4|s_1) \\ 
p_\pi(s_1|s_2) & p_\pi(s_2|s_2) & p_\pi(s_3|s_2) & p_\pi(s_4|s_2) \\ 
p_\pi(s_1|s_3) & p_\pi(s_2|s_3) & p_\pi(s_3|s_3) & p_\pi(s_4|s_3) \\ 
p_\pi(s_1|s_4) & p_\pi(s_2|s_4) & p_\pi(s_3|s_4) & p_\pi(s_4|s_4) 
\end{bmatrix}}_{P_\pi} 
\underbrace{\begin{bmatrix} 
v_\pi(s_1) \\ 
v_\pi(s_2) \\ 
v_\pi(s_3) \\ 
v_\pi(s_4) 
\end{bmatrix}}_{v_\pi}
$$
***
**策略评估（Policy Evaluation）**：给定一个策略，找到相应的状态价值
**贝尔曼方程（Bellman Equation）** 的矩阵-向量形式为

$$
v_\pi = r_\pi + \gamma P_\pi v_\pi
$$
有两种办法求解贝尔曼方程，以得到状态价值。
* **闭式解（Closed-Form Solution）** 为：

$$
v_\pi = (I - \gamma P_\pi)^{-1} r_\pi
$$
* **迭代解（Iterative Solution）** 为：

$$
v_{k+1} = r_\pi + \gamma P_\pi v_k
$$

该算法会产生一个序列 \(\{v_0, v_1, v_2, \dots\}\)。我们可以证明

$$
v_k \to v_\pi = (I - \gamma P_\pi)^{-1} r_\pi, \quad k \to \infty
$$
***
**动作价值（Action Value）**：从一个状态出发选择一个动作，得到的average return
定义：
$$
q_\pi(s, a) = \mathbb{E}[G_t|S_t = s, A_t = a]
$$
状态价值是动作价值的期望
由条件期望的性质可得

$$
\underbrace{\mathbb{E}[G_t|S_t = s]}_{v_\pi(s)} = \sum_a \underbrace{\mathbb{E}[G_t|S_t = s, A_t = a]}_{q_\pi(s, a)} \pi(a|s)
$$

因此，

$$
v_\pi(s) = \sum_a \pi(a|s)q_\pi(s, a)
$$
通过(1)式，可以推导出动作价值函数的表达式
$$
q_\pi(s, a) = \sum_r p(r|s, a)r + \gamma \sum_{s'} p(s'|s, a)v_\pi(s')\tag{2}
$$