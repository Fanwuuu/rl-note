**基于模型的（Model-Based）**：知道或学习环境的模型（状态转移 \(P\) 和奖励 \(R\)），可以利用模型做规划。
**无模型的（Model-Free）**：不知道也不显式学习环境模型，直接通过与环境交互的经验来学值函数或策略。
前面介绍的是model-based的情况，现在进入model-free的情况。策略迭代把基于模型的部分替换为不需要模型的部分，即得到这里的算法。
值迭代和策略迭代也被统称为model-based reinforcement learning，但更准确地来说，应该称之为动态规划的方法。
***
策略迭代的步骤2，策略改进时（通过$v_{\pi_k}$得到$v_{\pi_{k+1}}$），需要选择动作价值$q_{\pi_k}(s,a)$最大的动作作为策略，其中的**关键**就是$q_{\pi_k}(s,a)$。
我们试着套用策略迭代的框架来解决model-free情况下的问题。
总的来说，策略迭代法得到最优策略都是通过循环执行这个步骤：
$$
\underbrace{\text{计算当前策略状态价值}}_{\text{策略评估}}
\;\to\;
\underbrace{\text{计算当前策略下每个状态里动作价值最大的动作}
\;\to\;
\text{策略选择该动作（更新策略）}}_{\text{策略改进}}
$$
在model-free的情况下，即使得到了当前策略的状态价值$v_{\pi_k}$，也无法直接得到当前策略的动作价值。那么可不可以直接得到当前策略的动作价值，再直接通过动作价值更新策略，就不用进行状态价值的计算（即策略评估）了呢？
动作价值的这种表示形式是model-free的：
$$q_{\pi_k}(s,a)=\mathbb E[G_t|S_t=s,A_t=a]$$
用大数定理，通过大量采样，让样本均值逼近于期望。这样就在model-free的情况下得到了动作价值。
**蒙特卡洛动作价值估计的流程**：
* 从 \((s, a)\) 开始，遵循策略 \(\pi_k\)，生成一个 episode。
* 该 episode 的回报是 \(g(s, a)\)
* \(g(s, a)\) 是 \(G_t\) 的一个样本，位于

    $$
    q_{\pi_k}(s, a) = \mathbb{E}[G_t|S_t = s, A_t = a]
    $$

* 假设我们有一组 episode，因此得到 \(\{g^{(j)}(s, a)\}\)。那么，

    $$
    q_{\pi_k}(s, a) = \mathbb{E}[G_t|S_t = s, A_t = a] \approx \frac{1}{N} \sum_{i=1}^N g^{(i)}(s, a).
    $$
有了动作价值，可以绕过状态价值进行策略的更新。
**MC Basic Algorithm算法描述**：
给定一个初始策略 \(\pi_0\)，在第 \(k\) 次迭代中有两个步骤。
* **步骤 1：策略评估（Policy Evaluation）**。这一步是为了获得所有 \((s, a)\) 的 \(q_{\pi_k}(s, a)\)。具体来说，对于每个动作-状态对 \((s, a)\)，运行无数次（或足够多次）episode。它们的回报的平均值被用来近似 \(q_{\pi_k}(s, a)\)。
* **步骤 2：策略改进（Policy Improvement）**。这一步是为了求解
    \(\pi_{k+1}(s) = \arg\max_\pi \sum_a \pi(a|s)q_{\pi_k}(s, a)\)，对于所有 \(s \in \mathcal{S}\)。贪心最优策略是 \(\pi_{k+1}(a_k^*|s) = 1\)，其中 \(a_k^* = \arg\max_a q_{\pi_k}(s, a)\)。

**与策略迭代（Policy Iteration）算法完全一致，除了**
* 直接估计 \(q_{\pi_k}(s, a)\)，而不是求解 \(v_{\pi_k}(s)\)。
***
使用MC算法时，需要指定episode的长度。
episode的长度需要足够长，以使得智能体能够达到目标。但也不需要无限的长。
***
#### 让数据更高效
单个episode可以用于评估多个状态价值，例如：

$$
s_1 \xrightarrow{a_2} s_2 \xrightarrow{a_4} s_1 \xrightarrow{a_2} s_2 \xrightarrow{a_3} s_5 \xrightarrow{a_1} \dots \quad \text{[原始 episode]}
$$
$$
s_2 \xrightarrow{a_4} s_1 \xrightarrow{a_2} s_2 \xrightarrow{a_3} s_5 \xrightarrow{a_1} \dots \quad \text{[从 \((s_2, a_4)\) 开始的 episode]}
$$
$$
s_1 \xrightarrow{a_2} s_2 \xrightarrow{a_3} s_5 \xrightarrow{a_1} \dots \quad \text{[从 \((s_1, a_2)\) 开始的 episode]}
$$
$$
s_2 \xrightarrow{a_3} s_5 \xrightarrow{a_1} \dots \quad \text{[从 \((s_2, a_3)\) 开始的 episode]}
$$
$$
s_5 \xrightarrow{a_1} \dots \quad \text{[从 \((s_5, a_1)\) 开始的 episode]}
$$

可以估计 \(q_\pi(s_1, a_2), q_\pi(s_2, a_4), q_\pi(s_2, a_3), q_\pi(s_5, a_1), \dots\)
**数据高效方法（Data-Efficient Methods）**：
* **首次访问法（First-Visit Method）** 对于单个episode，只用第一次出现的$q_\pi(s,a)$进行估计，后续再次出现不使用。
* **每次访问法（Every-Visit Method）** 每次都进行估计。
#### 让策略更新更高效
基于 MC 的强化学习（MC-Based RL）中的另一个方面是**何时更新策略**。有两种方法。
* **第一种方法**是在策略评估步骤中，收集从某个状态-动作对开始的所有 episode，然后使用平均回报来近似动作价值。
    * 这是 MC Basic 算法所采用的方法。
    * 这种方法的问题在于，智能体必须等到所有 episode 都被收集完毕。
* **第二种方法**使用**单个 episode（Single Episode）** 的回报来近似动作价值。
    * 通过这种方式，我们可以**逐个 episode 地** 改进策略。

**广义策略迭代（Generalized Policy Iteration, GPI）**：
* 不是一个具体的算法。
* 它指的是在**策略评估（Policy-Evaluation）** 和**策略改进（Policy-Improvement）** 过程之间来回切换的通用思想或框架。
* 许多基于模型和无模型的强化学习（RL）算法都属于这个框架，例如值迭代/策略迭代/MC Basic。

综合上面两种优化，得到MC Exploring Starts算法
exploring要求每一个$(s,a)$都要被访问到
starts要求从每一个$(s,a)$开始一个episode
相对的，visits要求至少每一个$(s,a)$要被访问过
***
可以引入soft policy，把exploring starts去掉，不需要从每一个$(s,a)$出发。
如果采取**任何动作**的概率都是正数，则该策略被称为**软策略（Soft Policy）**。
$$
\left\{
\begin{matrix}
\text{determination} & \leftarrow & \text{greedy} \\
\text{stochastic} & \leftarrow & \text{soft}/\varepsilon\text{-greedy}
\end{matrix}
\right.
$$
* **\(\varepsilon\)-贪心策略（\(\varepsilon\)-Greedy Policy）**

$$
\pi(a|s) = \begin{cases} 
1 - \frac{\varepsilon}{|\mathcal{A}(s)|}(|\mathcal{A}(s)| - 1), & \text{对于贪心动作} \\ 
\frac{\varepsilon}{|\mathcal{A}(s)|}, & \text{对于其他 } |\mathcal{A}(s)| - 1 \text{ 个动作} 
\end{cases}
$$

其中 \(\varepsilon \in [0, 1]\) 且 \(|\mathcal{A}(s)|\) 是 \(s\) 的动作数量。
使用该策略是为了平衡探索和**充分利用（Exploitation）**。
只要把MC Exploring Starts策略更新步骤的greedy替换成$\varepsilon$-greedy即可。

与贪心策略相比，
* **\(\varepsilon\)-贪心策略**的**优势**在于它们具有更强的探索能力，因此不需要探索启动条件。
* **劣势**在于 \(\varepsilon\)-贪心策略通常**不是最优的**（我们只能证明总是存在最优的贪心策略）。
    * MC \(\varepsilon\)-贪心算法给出的最终策略仅在所有 \(\varepsilon\)-贪心策略的集合 \(\Pi_\varepsilon\) 中是最优的。
    * \(\varepsilon\) 不能太大。

在实际操作中，可以让$\varepsilon$逐渐趋向于0，最后得到的策略就有比较好的最优性。
***
比较不同的$\varepsilon$值训练出来的策略的状态价值，$\varepsilon$越高，状态价值越低，最优性变得越来越差。
可以看到$\varepsilon=0$和$\varepsilon=0.1$得到的策略是一致的（consistent），但$\varepsilon=0.2$和$\varepsilon=0.5$时，就和最优的greedy策略不一致了。这告诉我们$\varepsilon$不能太大，或者说一开始让$\varepsilon$比较大，探索性比较强，后面逐渐减小到0，让其得到一个最优的策略。

在得到optimal $\varepsilon$-greedy策略之后，在实际应用中不会再带着$\varepsilon$，而是转化为确定性的策略。