**状态（State）**：智能体相对于环境的状态
**状态空间（State Space）**：所有状态的集合
$$\mathcal{S}(s_i)=\left\{s_i\right\}_{i=1}^n$$
其中，$n$为状态数量。
***
**动作（Action）**：每个状态可采取的行动
**状态的动作空间（Action Space）**：一个状态所有可能动作的集合
$$\mathcal{A}(s_i)=\left\{a_i\right\}_{i=1}^n$$
其中，$n$为状态$s_i$可能的动作数量。
动作空间和状态依赖，不同状态的action space不一样。
***
**状态转移（State Transition）**：采取一个动作，智能体从一个状态转移到另一个状态。
例如：
$$s_1\xrightarrow{a_2}s_2$$
可以使用表格表示状态转移
| | a1 (upwards) | a2 (rightwards) | a3 (downwards) | a4 (leftwards) | a5 (unchanged) |
|---|---|---|---|---|---|
| s1 | s1 | s2 | s4 | s1 | s1 |
| s2 | s2 | s3 | s5 | s1 | s2 |
| s3 | s3 | s3 | s6 | s2 | s3 |
| s4 | s1 | s5 | s7 | s4 | s4 |
| s5 | s2 | s6 | s8 | s4 | s5 |
| s6 | s3 | s6 | s9 | s5 | s6 |
| s7 | s4 | s8 | s7 | s7 | s7 |
| s8 | s5 | s9 | s8 | s7 | s8 |
| s9 | s6 | s9 | s9 | s8 | s9 |

更一般的情况，用状态转移概率表示状态转移
直观理解：在状态 \(s_1\) 下，如果我们选择动作 \(a_2\)，那么下一个状态是 \(s_2\)。
**数学表示**：例如

$$
p(s_i|s_1, a_2) = 
\begin{cases} 
1, & i = 2 \\ 
0, & \forall i \neq 2 
\end{cases}
$$

***
**策略（Policy）** 告诉智能体在某个状态选择什么动作
**数学表示**：例如，对于状态 \(s_1\)：
$$
\begin{aligned}
\pi(a_1|s_1) &= 0 \\
\pi(a_2|s_1) &= 1 \\
\pi(a_3|s_1) &= 0 \\
\pi(a_4|s_1) &= 0 \\
\pi(a_5|s_1) &= 0
\end{aligned}
$$
这是确定性的策略，也有随机性的策略。
策略也可以用表格的形式表达出来，如状态转移表一般。
***
**奖励（Reward）**：我们采取一个行动后得到的一个实数
奖励也可以用表格表示，但奖励可能不确定，此时用表格无法表示
**数学描述**：条件概率
* **直观理解**：在状态 \(s_1\) 下，如果我们选择动作 \(a_1\)，奖励为 \(-1\)。
* **数学表达**：\(p(r = -1|s_1, a_1) = 1\) 且 \(p(r \neq -1|s_1, a_1) = 0\)
***
**轨迹（Trajectory）**：一个状态-动作-奖励链
$$
s_1 \xrightarrow[r=0]{a_2} s_2 \xrightarrow[r=0]{a_3} s_5 \xrightarrow[r=0]{a_3} s_8 \xrightarrow[r=1]{a_2} s_9
$$
轨迹的**回报（Return）** 指轨迹的所有奖励的和。
回报可以用于评估一个策略是好是坏
轨迹无限持续下去，回报会趋于无穷大，故引入**折扣因子（Discount Rate）**$\gamma\in[0,1)$
例如
$$
\begin{aligned}
\text{discounted return} &= 0 + \gamma 0 + \gamma^2 0 + \gamma^3 1 + \gamma^4 1 + \gamma^5 1 + \dots \\
&= \gamma^3 (1 + \gamma + \gamma^2 + \dots) = \gamma^3 \frac{1}{1 - \gamma}.
\end{aligned}
$$
作用：1) 使求和变得有限；2) 平衡远期和近期奖励

当遵循某个策略与环境进行交互时，智能体可能会停在某些**终止状态（Terminal States）**。由此产生的轨迹被称为一个**回合（Episode）**（或一次**试验（Trial）**）。.
一个episode通常被假定为一条**有限轨迹（Finite Trajectory）**。具有回合的任务被称为**分幕式任务（Episodic Tasks）**。
有些任务没有终止状态，会持续下去，叫做**持续性任务（continuing tasks）**
episode 是 trajectory 的一个特例（有限且带终止信号）
可以把episodic tasks转化成continuing tasks
* 选项 1：将目标状态视为一个特殊的**吸收状态（Absorbing State）**。一旦智能体到达吸收状态，它将永远不会离开。随之而来的奖励 \(r = 0\)。
* 选项 2：将目标状态视为一个带有策略的**正常状态（Normal State）**。智能体仍然可以离开目标状态，并在进入目标状态时获得 \(r = +1\) 的奖励。
***
**马尔可夫决策过程（Markov decision process, MDP）**
MDP 的关键要素：
* **集合**：
    * 状态：状态集合 \(\mathcal{S}\)
    * 动作：与State \(s \in \mathcal{S}\) 相关联的动作集合 \(\mathcal{A}(s)\)。
    * 奖励：奖励集合 \(\mathcal{R}(s, a)\)。
* **概率分布**：
    * 状态转移概率：在状态 \(s\) 下采取动作 \(a\)，转移到状态 \(s'\) 的概率为 \(p(s'|s, a)\)
    * 奖励概率：在状态 \(s\) 下采取动作 \(a\)，获得奖励 \(r\) 的概率为 \(p(r|s, a)\)
* **策略**：在状态 \(s\) 下选择动作 \(a\) 的概率为 \(\pi(a|s)\)
* **马尔可夫性质**：无记忆性（Memoryless Property），即只与当前状态有关
$$
\begin{aligned}
p(s_{t+1}|a_{t+1}, s_t, \dots, a_1, s_0) &= p(s_{t+1}|a_{t+1}, s_t), \\
p(r_{t+1}|a_{t+1}, s_t, \dots, a_1, s_0) &= p(r_{t+1}|a_{t+1}, s_t).
\end{aligned}
$$
如果策略是确定性策略，马尔科夫决策过程则变为马尔科夫过程